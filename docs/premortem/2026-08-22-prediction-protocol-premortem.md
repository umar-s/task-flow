# Premortem: `prediction-protocol` (design-spec v1)

**Дата:** 2026-08-22 · **Объект:** `docs/superpowers/specs/2026-08-22-prediction-protocol-design.md`
в редакции v1 (до первой строки `hooks.json`, как требовал §10 спеки) плюс
справка по платформе хуков — документация Claude Code и **эмпирические пробы**
на 2.1.239 (см. «Что проверено руками»).
**Углы:** Исполнитель · Противник («зелёный по форме») · Платформа · Интеграция
(task / loop-foundry / co-rar / ci-gate) · Журнал и метрики · Владелец и вес —
затем панель из трёх рефутеров (по тексту спеки / по воспроизведению / по
последствию, голосование 2 из 3) и критик полноты.
**Формат:** Workflow, 10 агентов в чистом контексте, 24 мин; 60 находок после
дедупа по заголовку; панель оставила 20 (+12 спорных, по одному голосу), 28
отвергла — все отвергнутые оказались повторами с другим якорем (ни одной
находки не опровергли по существу). Триаж и решения — владельцем флоу.

Отличие от премортемов 1.9.0/1.10.0: объект — спека без черновика кода, и
панель получила не только текст, но и факты платформы. Две трети принятых дыр
— «спека обещает механизм, которого нет ни в дереве поставки, ни у платформы»
(receipt без привязки, грейдинг без грейдера, fail-closed без trap'а,
классификатор не того объекта). Результат — не правки v1, а v2 с другим центром
тяжести: CLI `predict` как harness, хук — тонкая сверка.

## Что проверено руками (до панели)

Project-level PreToolUse-хук на `Bash`, `claude -p`, haiku, 12 вызовов:

| хук | `-p --allowedTools Bash` | `-p --dangerously-skip-permissions` |
|---|---|---|
| `permissionDecision: deny` | блок, причина уходит модели | блок |
| `exit 2` + stderr | блок | блок |
| `permissionDecision: ask` | блок (некому ответить) | блок |
| `exit 1` + stderr | **команда выполнилась** | выполнилась |
| битый JSON | **выполнилась** | выполнилась |
| таймаут хука | **выполнилась** | выполнилась |

stdin: `session_id, transcript_path, cwd, prompt_id, permission_mode, tool_name,
tool_input{command, description}, tool_use_id`; у вызова из субагента — те же
`session_id` плюс `agent_id`, `agent_type` (хук **срабатывает** в субагентах).
Окружение процесса хука: `CLAUDE_PROJECT_DIR`, `CLAUDE_CODE_SESSION_ID`,
`CLAUDE_CODE_ENTRYPOINT` (`cli` в интерактиве, `sdk-cli` в `-p`);
`CLAUDE_PLUGIN_ROOT` экспортируется плагинным хукам (superpowers читает его из
env в `hooks/session-start`). Эти шесть фактов легли в §5 спеки v2 напрямую.

## Принято (закрыто в спеке v2)

| # | Дыра (панель) | Решение в v2 |
|---|---|---|
| H-1 | P-1/P-12: receipt не привязан ни к команде, ни к сессии — один открытый receipt открывает любую one-way команду на 30 мин из любой сессии; halt не исполняется хуком | `predict open --action '<cmd>'`; хук сравнивает нормализованную `tool_input.command` с receipt'ом **этой сессии** (`session_id` со stdin), одноразовость (`consumed`), один receipt в полёте, TTL от штампа скрипта |
| H-2 | P-2/P-15/P-24/P-36/P-51/P-52: «один список, два потребителя» — `SQL_RE`/`ORM_RE` матчат тело файла миграции, one-way-doors — прозу вопросов; на командной строке это FP на `grep 'DELETE FROM'`, `git branch --delete`, и FN на `alembic upgrade head`, `systemctl restart`, `gh pr merge` | Свой классификатор командной формы (`lib/classify.sh`): глагол по позиции, разбиение составных команд, снятие `sudo`/`env`, `bash -c` рекурсивно; DDL-словарь общий только для ветки inline-SQL; корпуса must-deny и must-pass в `tests/`; `--also` для обёрток проекта |
| H-3 | P-3/P-20/P-21: платформа fail-open (exit 1, битый JSON, таймаут → действие идёт), `set -euo pipefail` + отсутствующий `jq` = гейт молча выключен | `trap` ERR/EXIT → deny-JSON + `exit 2`; единственный явный путь к exit 0; внутренний дедлайн ≤ 10 с при `timeout` 15; парсер `python3 → jq → deny`; негативные контроли на путях ошибок (§10.6) |
| H-4 | P-4/P-17/P-27/P-56: переменная обхода — либо навсегда в `.bashrc`, либо в строке команды, то есть её пишет та же модель; кто пишет запись `bypass` — не сказано | Переменной нет. Обход = клик человека на `ask` и `predict off --reason` (пишет `bypass` в журнал, переводит сессию «вне области»). В headless обхода нет |
| H-5 | P-5/P-53: причина deny — единственная инструкция модели в сессии, не читавшей SKILL; ритуал ~9 вызовов с чтением references | Причина — шаблон: версия, путь к `predict`, строка `predict open …` с подставленной командой; ритуал — 3 вызова (`open` → действие → `close`), references читать не нужно |
| H-6 | P-6: merge/deploy/cache-flush — фаза 8 под `land.md`, куда routing не шёл; `land.md` §6 велит «стоп на permission denied» | Routing в `land.md` §1–§2 (наблюдаемые там уже названы) и §6 («deny гейта — не permission denied: receipt и повтор один раз»); §7 спеки — чеклист поставки 1.11.0 |
| H-7 | P-7/P-13/P-19/P-33/P-44/P-45: hit-rate нечем считать (скрипта нет в дереве), «окно» не определено, INCONCLUSIVE и незакрытые receipt'ы выпадают из знаменателя, в лупах журнал `ladder.md` не имеет поля | `predict report` — единственный источник строки; `rate = HIT/(HIT+MISS+INCONCLUSIVE)`, `O open`, `B bypass`, `n<K → insufficient`; INCONCLUSIVE останавливает как MISS до `retry`/`withdraw`; в лупах — поле `predictions` в существующем jsonl, один журнал |
| H-8 | P-8/P-16/P-25: matcher `mcp__.*__Bash` покрывает несуществующую форму; реальные MCP-инструменты записи без `command` либо не гейтятся, либо fail-closed режет все чтения | 1.0: matcher литерал `Bash`; MCP и PowerShell вне области, заявлено |
| H-9 | P-9/P-30/P-40/P-48: `actual` «дословно» (§3) против «усечённо» (§6), скан «если есть» = fail-open у раковины, журнал пишется через Write, которого matcher не видит | `actual` пишет только `predict close`: значение + ≤ 5 строк/512 байт, встроенные маски всегда, gitleaks когда есть, `redaction:` в записи; значения переменных окружения не попадают |
| H-10 | P-10/P-14: «разобрать команду» не определено; после одного deny модель уносит SQL в файл через Write и запускает `psql -f` | Непрозрачный ввод (`-f`, `<`, heredoc), `ssh`/`docker exec`/`kubectl exec` — one-way по построению; `bash file.sh`/`make` — названный предел + `--also` + правило «обёртка ради обхода = bypass» + ревью читает журнал |
| H-11 | P-11/P-50: `exit code 0` — легальный observable по тексту спеки; тавтология проходит lint и гейт; шум на точном литерале даёт ложные опровержения | Закрытая грамматика `--expect`; для destructive `observe ≠ action` и не только `exit=`; диапазон и regex в грамматике; MISS подтверждается повторным прогоном |
| H-12 | P-18: журнал — редактируемый моделью Markdown; falsifier/prediction переписываются после `actual`; advisory-receipt пишется после взгляда | Receipt живёт в state вне репо, штамп и sha — скриптом; хук при `consumed` проверяет sha; в репо попадает только закрытая запись, которую рендерит `close` |
| H-13 | P-22/P-23/P-31/P-59: сессионно-глобальный хук + fail-closed + журнал по `TASK-ID` = deny во всех проектах без задачи; единственный выключатель — снести плагин | Область = сессия с `predict on` (состояние вне репо, по `session_id`); вне области one-way → `ask` (O7 аудита), нерелевантное → exit 0; fail-closed действует в области |
| H-14 | P-35: loop-foundry не может исполнить контракт — плагина нет в чекауте runner'а, два журнала с двумя схемами, halt некому исполнять в headless | Контракт §7: `predict on --loop` из `run.sh`, GAPS-пункт «CLI доступен, доказано selftest», `predict report --json` → поле `predictions` в jsonl, halt = состояние сессии (хук) + `predict status` для `gates.sh` |
| H-15 | P-37/P-41/P-39: «машинный грейдинг» без машины — модель вставляет `actual` и ставит `verdict`; co-rar Critic — Red Team без интерфейса грейдера, «нечёткий» решает модель | `predict close` исполняет observe и ставит вердикт; модель не пишет `actual`/`verdict` (lint: только со штампом скрипта); несравнимое = INCONCLUSIVE; co-rar из 1.0 убран. Слой назван честно: хук — deterministic по форме, грейдер — по значению |
| H-16 | P-43: «falsifier, недостижимый для той же команды» — неразрешимо grep'ом | Falsifier не авторится — выводится как дополнение утверждения на той же команде; lint проверяет только грамматику |
| H-17 | P-47/P-58: леджер Refuted write-only — в файле задачи, который следующая задача не открывает; ничто не требует строки на каждый MISS | Один `docs/evidence/REFUTED.md` на репо, пишет только `predict ack`, lint требует строку на каждый MISS; читатель назван — `task` фаза 0, loop-foundry Phase 0 |
| H-18 | P-55/P-29: ветка «с receipt» не описана; симметричный `allow` снимает клик человека на `push --force`; в headless «нет решения = deny» толкает к `allow` на все команды | Таблица режимов §5: с receipt — `ask` с прогнозом в причине (интерактив) / exit 0 (headless); `allow` запрещён lint-grep'ом; нерелевантное — exit 0 пусто |
| H-19 | P-57: advisory-скоуп — «самая ценная территория» — без механизма; отсутствие записи невидимо, hit-rate advisory — самоотбор | Объект со счётчиком: каждый `live`-пункт DoD закрывается через advisory-цикл до взгляда, `DoD-n` цитирует `predict <id>`; пункт без id виден как пункт без улики |
| H-20 | P-28: режим ждали флагом от loop-foundry, хотя `agent_id`/`ENTRYPOINT` уже есть | Режим хук читает сам: `agent_id` → субагент, `CLAUDE_CODE_ENTRYPOINT=cli` → интерактив, иначе headless |
| H-21 | P-49: `<utc>` и `<id>` в заголовке печатала модель — ложные «истёк» по таймзоне или вечные receipt'ы | Штампует скрипт; хук принимает только записи state, которые пишет `predict` |
| H-22 | P-38: кросс-плагинный контракт без пина — routing 1.11.0 парсит формат журнала другого репо | Потребители не парсят файл — вызывают `predict report`/`predict on`; заголовок несёт `contract=1` |
| H-23 | P-46: открытые receipt'ы в отслеживаемом файле — шум коммитов, гипотезы автора в диффе ревью | Открытые — в state вне репо; в репо — только закрытые записи, коммит в фазе 8 при закрытии |
| H-24 | P-60: одно правило в двух репо — команда `live`-DoD, stop-условие Reversibility, evidence-блок, receipt | `live`-строка DoD цитирует `predict <id>`, stop-условие deploy = falsifier deploy-receipt'а (§8) |
| H-25 | P-34: «routing условный» без шага установления — протокол пересказан, а не исполнен, и выглядит одинаково | `predict on` печатает `predict-gate: active v… (selftest: deny)` / `absent (…)` — то же правило, что `gate: absent`; строка `report` допустима только рядом с `active` |
| H-26 | Критик: §10 проверяет только «отказал» — нет must-pass, нет путей ошибок, нет прогона через платформу, у lint нет точки вызова | §10 — матрица из 12 строк; `platform-probe.sh` ручной; lint вызывается из `close` и `report` |
| H-27 | Критик: хук срабатывает в ревьюерах-субагентах, а причина deny велит read-only агенту писать в журнал автора | Ветка `agent_id`: deny «report as a finding, do not write a receipt»; строка в шаблонах ревью 6/6b; фикстуры stdin с `agent_id` |
| H-28 | Критик: «вне рабочего каталога сессии» хук вычислить не может; провенанс уже правило человека в `land.md` §7 / CLAUDE.md | Оговорка удалена из §4; `rm -r*` — one-way кроме путей под `/tmp`/`$TMPDIR`; вне области — `ask`, как и велит правило владельца |
| H-29 | Критик: три механизма ARC отброшены молча (batch, Verified/Assumed, грамматика утверждений) | §1 называет все три и чем заменён каждый; грамматика утверждений принята |
| H-30 | Критик: консьюмерская сторона (1.11.0) названа без своих домашних правил — lint-grep, пара README, релиз | §7 — чеклист поставки 1.11.0 с lint, README, CHANGELOG, тегом |
| H-31 | Критик: Windows/PowerShell молча вне гейта, `LC_ALL=C` не в чеклисте | Платформа заявлена в §2; `LC_ALL=C` и инвентарь инструментов — в `hook-engineering.md` |
| H-32 | Критик: поставка с `main` без пина, без release-gate и без выключателя — ровно тот артефакт, который self-check линейки должен ловить | Версия в каждой причине, CI-матрица до `main`, `predict off` как аварийный выход; пин тега в каталоге **не** принят (см. R-2) |

## Отвергнуто или принято частично

| # | Находка | Почему |
|---|---|---|
| R-1 | P-46 (часть): исключить `docs/evidence/` из диапазона диффа ревью 6 | Закрытая запись журнала — артефакт флоу того же рода, что спека и premortem edges; ревьюер их получает. Открытые receipt'ы в дифф не попадают (H-23), этого достаточно |
| R-2 | Критик: пин тега в `marketplace.json` для этого плагина | Противоречит конвенции каталога («ref не пинится, релизного шага нет»); исключение ради одного плагина — второе правило для того же места. Компенсация: версия в причине, CI-матрица, `predict off` |
| R-3 | P-50 (часть): обязательный `--baseline` для destructive | Дополнительный прогон измерения на каждое действие — вес; оставлен опциональным с WARNING в lint на «прогноз равен baseline» |
| R-4 | P-57 (часть): `journal-lint` читает таблицу DoD спеки и печатает `live items unpredicted: K` | Кросс-плагинный парсинг чужого артефакта — именно то, что H-22 запрещает; покрытие видно в evidence-блоке по отсутствию `predict <id>` у пункта |
| R-5 | P-26/P-37 (часть): «гейт непроходим ровно на один Write» — receipt пишет та же модель, значит слой не deterministic | Принято как формулировка (слой назван честно), не как отказ от гейта: по форме хук deterministic, по значению грейдер deterministic; что модель авторит гипотезу — свойство задачи, а не дефект. ARC тоже принимал прогноз от модели |
| R-6 | P-28 (часть): субагент с receipt → `deny` вместо «пусть оркестратор решит» | Принято как есть: субагент one-way дверь не открывает, это делает оркестратор — O8-light, дешевле, чем второй канал |

## Ревью-панель по черновику (1.0.0, до релиза)

Замороженная копия плагина (`cp -a`, без `.git`), 6 измерений — гейт,
классификатор, CLI, соответствие спеке, SKILL как единственная инструкция,
безопасность и supply chain — затем три рефутера (по воспроизведению, по коду,
по последствию; голосование 2 из 3) и критик полноты. 74 находки после дедупа,
**60 выживших**, 12 спорных, 2 отвергнутых. Первый запуск панели лёг целиком на
лимите сессии (сброс 16:00), второй — 38 мин, 1,6 M токенов.

Принято и закрыто в коде до релиза (полный список правок — спека §12):

| # | Находка (рефутеры) | Решение |
|---|---|---|
| RV-1 | R-1: control-байт в команде → невалидный deny-JSON → платформа пропускает | `pp_json_str` экранирует все байты < 0x20 и DEL; deny по двум каналам (JSON + stderr, exit 2); контроль в сьюте пропускает ответ через JSON-парсер |
| RV-2 | R-2: «один receipt в полёте» не исполнялся — `break` на первом совпадении | Два прохода: сначала все потреблённые, потом совпадение; фикстура с двумя открытыми receipt'ами |
| RV-3 | R-3/R-70: нет `lib/common.sh` → `exit 1` → пропуск | Outer-обёртка без зависимостей, `deny_literal` до любого `source`, контроль «lib missing» |
| RV-4 | R-5/критик: GNU `timeout` как жёсткая зависимость | `pp_with_deadline`: `timeout` → `gtimeout` → bash-watchdog; контроль без `timeout` в PATH |
| RV-5 | R-6/R-71: предсказуемый `/tmp/pp-gate-err.$$` | `mktemp`, при сбое — без захвата stderr; решение никогда не зависит от временного файла |
| RV-6 | R-7/R-14: линейная стоимость на строках heredoc, тела heredoc классифицируются | Префильтр словаря до разбора, тела heredoc выбрасываются (`<<`-строка остаётся), лимит 400 сегментов |
| RV-7 | R-8/R-53: `consumed` пишется до ответа человека — `close` грейдит невыполненное действие | PostToolUse `predict-fired.sh`; `close` без `fired` отказывает и советует `withdraw --reason 'not fired'` |
| RV-8 | R-9: `PP_NOW` в командной строке модели сдвигает штампы | `PP_NOW` только под `PP_TEST=1`; `opened` входит в sha; будущий штамп = истёк |
| RV-9 | R-12/R-13/R-57: глобальные опции перед глаголом, слипшиеся флаги | `_pp_strip_global_opts` по инструментам; `-[A-Za-z]*[ceQ]`, `-[A-Za-z]*X`, `-d@`, `-fu` |
| RV-10 | R-15/R-16/R-17: `$(…)` съедал хвост, `then/do/&` не режутся, `curl \| bash` проходит | Depth-aware извлечение подстановок с `@SUBST@`, quote-aware сплиттер, stdin-fed shell'ы/интерпретаторы = opaque-exec |
| RV-11 | R-18/R-19/R-44: `;` внутри `-c`, sqlite3/sqlcmd/mongosh позиционные, `\i`, FLUSH, write-CTE | sqlarg из сырой строки по всем statement'ам; `explain`/`with` убраны из allowlist; meta-команды чтения файла = opaque input |
| RV-12 | R-20: `--help` где угодно снимал классификацию | Только первым/вторым аргументом после глагола и не для ssh/exec |
| RV-13 | R-21/R-22: read-only глаголы облаков, `rollout status`, `fdisk -l`, dry-run | Allowlist'ы чтения, `_pp_has_dry_run`, `dd of=/dev/null` |
| RV-14 | R-23/R-24/R-25: `su -c`, `setsid`, `flock -c`, `\git`, `'git'`, `gi""t`, `$FORCE`, `reflog expire` | Расширены префиксы и дедублирование токенов; `$`-флаг у `git push` = vcs-force |
| RV-15 | R-26: `stdout-in=` падал на объявлении `local` | Исправлено, контроль в сьюте |
| RV-16 | R-27/R-28/R-45: halt снимался HIT'ом и повторным `on`; `withdraw`/`retry` принимали MISS | Halt снимает только `ack`/`retry`/`withdraw` по правилам; `on` держит halt и возвращает 1 |
| RV-17 | R-29/R-30/R-55/R-62: леджер лупа не там, `predict on <loop> --loop` падал | `ledger_path_for(root)` один на репо; `--task`/`--loop` — флаги вида, имя позиционное |
| RV-18 | R-31/R-73: коллизии id по 8-символьному префиксу имени сессии; id как путь и regex | `sha8(session)-seq`; валидация id и имени сессии; точное совпадение в `awk` |
| RV-19 | R-32/R-41/R-68: JSON-секреты, усечение до масок, гипотеза/expect без редакции | Маска JSON-ключей; редакция до усечения; все авторские поля через `pp_redact`; секретная форма — отказ при `open` |
| RV-20 | R-33/R-48: ненулевой exit измерения с выводом — MISS вместо INCONCLUSIVE | По §3: INCONCLUSIVE; `http=000` — INCONCLUSIVE; `pipefail` **не** включён (ломает `ls … \| wc -l`) |
| RV-21 | R-34/R-40/R-54: `close`/`report` не запускали lint | `close` линтит журнал, `report` печатает `lint=` и возвращает 1 |
| RV-22 | R-35/R-36/R-67: переносы строк в полях и `awk -v` — подделка записей | Все поля однострочные; `awk` через `ENVIRON` |
| RV-23 | R-38/R-47: `active=1` до selftest | selftest первым; при `broken` сессия остаётся вне протокола, rc 2 |
| RV-24 | R-39/R-50: параллельные писатели, `close-without-gate` невидим | `flock`, строка `gated:` в записи, `U ungated` в `report`, WARNING lint |
| RV-25 | R-42/R-43/R-60: дедлайн на нерелевантной команде, parse-failure = `ask` vs спека | Префильтр решает «нерелевантно» до дедлайна; parse-failure на one-way — deny |
| RV-26 | R-46: 4-я колонка REFUTED.md — имя журнала, не «где» | `ack --where` обязателен; колонка `where`, журнал пятой |
| RV-27 | R-49: строки §10 без тестов | Корпуса прогоняются через гейт; субагент с receipt; lint-кейсы; `broken` hook |
| RV-28 | R-51: `allow` ловился только в кавычках | Word-match grep без комментариев |
| RV-29 | R-58: INCONCLUSIVE «почини измерение» без легального пути | `retry --observe … [--expect …]` |
| RV-30 | R-59/R-61/R-64: fallback-путь SKILL, deny без пути к CLI, вес описания | `$PREDICT` единственный источник; путь во всех причинах; описание ≤ 90 слов (lint) |
| RV-31 | R-63: числа в README | Счётчики убраны, корпус сам печатает |
| RV-32 | R-66: `--observe` — негейченный канал исполнения | `check_observe`: классификация + обёртки; `predict` в измерении запрещён |
| RV-33 | R-69 (спорная): состояние подделываемо | Принято частично: guard-хук на Edit/Write, класс `state-tamper` для Bash, `fired` обязателен; sha + `opened` в хэше |
| RV-34 | R-72/R-74: временные файлы CLI, CI ниже домашнего стандарта | `umask 077`, один `mktemp -d` на `close`; CI по образцу task-flow |
| RV-35 | Критик: потребитель `"${PREDICT:?}"` без продюсера | SessionStart `session-env.sh` экспортирует `PREDICT` (уже было в коммите после заморозки копии) |
| RV-36 | Критик: headless-runner и хук ключуются разными id | `PP_SESSION` из окружения хука + `claude -p --session-id`; задокументировано |
| RV-37 | Критик: `predict-gate: active` доказывает скрипт, не регистрацию | Liveness-штамп `hook-seen`; `status`/`report` печатают `gate: seen` |
| RV-38 | Критик: `predict open --action '… && …'` отказывал сам себе | Вызовы `predict` исключены из классификации |
| RV-39 | Критик: журнал коммитится неясно когда и откуда | `predict on` после ветвления; `land.md` §5 — коммит по пути при закрытии (PR #4) |
| RV-40 | Критик: macOS заявлен без тестов; `validate .` не работает; личный путь в CLAUDE.md | README: Linux, macOS не тестировался; `validate plugins/prediction-protocol`; путь убран, lint на личные пути |

Отвергнуто: R-10/R-11 (fallback на `CLAUDE_CODE_SESSION_ID` добавлен, но
«изменение платформы» не событие 1.0), R-37 (двойной прогон при MISS — по
спеке; обе попытки теперь записаны), R-56 (placeholder рецепта переставлен,
`exit=N` последним), R-64 частично (таблица режимов остаётся в SKILL — это
routing-контракт).

## Что проверено после ревью (до релиза 1.0.0)

- **Acceptance через установленный плагин** (`claude plugin install
  prediction-protocol@devpowers`, `claude -p`, sonnet, Claude Code 2.1.239),
  три прогона: `PREDICT` экспортирован SessionStart-хуком; под протоколом
  `rm -rf ./build` получил deny с рецептом, модель сама открыла receipt по
  рецепту, повторила команду, PostToolUse пометил `fired` реальным
  `tool_use_id`, `close` → HIT, `report` → `gate: seen`. Журнал — в форме
  контракта. Между прогонами обнаружено: `claude plugin update` не
  перетягивает код при той же версии — переустановка.
- `tests/platform-probe.sh` как релизный тест: 6/6 строк таблицы платформы.
- Вес: `claude plugin details` — ~320 токенов always-on, ~3k на вызов.

## Что не проверялось

- Поведение `permissionDecision: ask` в **интерактивной** сессии (виден ли
  текст причины в промпте, остаётся ли клик, как ведёт себя auto-режим) —
  все прогоны были headless; первая интерактивная сессия с установленным
  плагином — это и есть проверка.
- macOS (`gtimeout`/watchdog, BSD `sed`/`date`) — заявлено как
  «не тестировалось».
- Экспорт `CLAUDE_PLUGIN_DATA` — в спеке не используется.
